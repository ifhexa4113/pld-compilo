sudo: required
language: C
notifications:
  slack: thecursed:MCb6TtcwBk4XLoA1g31rmffY

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install bison flex g++-5

before_script:
  - g++ --version

script:
  - make OS=unix tests